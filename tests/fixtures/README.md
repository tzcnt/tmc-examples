Contains test fixtures - currently just hwloc topology XMLs to allow consistent testing on different environments.
These XMLs are embedded as strings rather than as XML files to avoid complications finding the files relative
to the working directory on different platforms.