name: x64-linux-gcc

on:
  push:
    branches: [ main, tests ]
  pull_request:
    branches: [ main ]

env:
  PRESET: gcc-linux-debug

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: cmake -G "Unix Makefiles" --preset ${{env.PRESET}} .
    - name: build
      run: cmake --build ./build/${{env.PRESET}} --parallel $(nproc) --target tests
    - name: run tests
      run: ./build/${{env.PRESET}}/tests/tests
